# Copy this file to .env.local and fill in the values.
# This file is committed so the team can quickly configure local dev.

# Required: OpenRouter API key (server-side only; do NOT prefix with NEXT_PUBLIC_)
OPENROUTER_API_KEY=sk-or-REPLACE_ME

# Optional: choose an OpenRouter model (see https://openrouter.ai/models)
# Defaults to openai/gpt-4o-mini if unset.
OPENROUTER_MODEL=openai/gpt-4o-mini

# Public site URL for attribution headers to OpenRouter
# Use your local dev port or production domain.
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Optional: If you proxy chat elsewhere, set a custom webhook endpoint.
# The ChatWidget defaults to /api/ai/openrouter when not set.
# NEXT_PUBLIC_CHAT_WEBHOOK=/api/ai/openrouter

