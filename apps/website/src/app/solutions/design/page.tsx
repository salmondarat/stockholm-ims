import SolutionDetail from "@/components/SolutionDetail";

export default function Page() {
  return (
    <SolutionDetail
      spec={{
        title: "Interior Design",
        desc: "Visually track inventory across locations and projects.",
        image: "/mock-media.svg",
      }}
    />
  );
}
