{
  "framework": "nextjs",
  "buildCommand": "SKIP_ENV_VALIDATION=1 pnpm build --filter=dashboard",
  "outputDirectory": "apps/dashboard/.next",
  "installCommand": "pnpm install --no-frozen-lockfile"
}
